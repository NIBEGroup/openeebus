[pytest]
addopts = --html=report.html --self-contained-html
markers =
    simopen: tests requiring SHIP_NODE_DEBUG=1 build flag
    debug_flags: tests requiring EEBUS_SERVICE_DEBUG=1 and SHIP_NODE_DEBUG=1 build flags
    three_node: tests requiring heat_pump, hems, and ev_charger binaries
